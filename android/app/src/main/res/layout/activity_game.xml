<?xml version="1.0" encoding="utf-8"?>
<!--
    The game renders into its own surface underneath; this activity only stacks the
    touch overlay (added in code) and a small status line on top of it.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/game_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:keepScreenOn="true">

    <TextView
        android:id="@+id/game_status_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/game_starting"
        android:textColor="@color/text_primary"
        android:textSize="16sp" />

</FrameLayout>
